---
name: grill-with-docs
description: Relentlessly interview the user to sharpen a SídloFlow test plan or design while maintaining the domain glossary and recording only genuinely durable decisions. Use when the user invokes /grill-with-docs before writing a Test Brief or test.
---

Run a `/grilling` session using the `/domain-modeling` skill. Resolve one decision at a time, update `CONTEXT.md` as domain terms crystallize, and create an ADR only when the domain-modeling criteria require one. Do not implement the plan until the user confirms shared understanding.

